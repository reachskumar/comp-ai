apiVersion: apps/v1
kind: Deployment
metadata:
  name: sync-worker
  namespace: compportiq
  labels:
    app: sync-worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sync-worker
  template:
    metadata:
      labels:
        app: sync-worker
    spec:
      serviceAccountName: compportiq-api
      # TODO: Move to worker node pool when spot instances are available
      # tolerations:
      #   - key: "workload"
      #     value: "sync"
      #     effect: "NoSchedule"
      # nodeSelector:
      #   pool: workers
      containers:
        - name: sync-worker
          image: asia-south1-docker.pkg.dev/compportiq/compportiq-prod-docker/api:latest
          # Same image + entrypoint as API — BullMQ runs inside NestJS
          command: ["node"]
          args: ["--max-old-space-size=4096", "--experimental-require-module", "apps/api/dist/main.js"]
          resources:
            requests:
              cpu: "1000m"
              memory: "2Gi"
            limits:
              cpu: "4000m"
              memory: "8Gi"
          env:
            - name: NODE_ENV
              value: "production"
            - name: API_PORT
              value: "4001"  # Different port, not exposed
            - name: LOG_LEVEL
              value: "info"
            - name: COMPPORT_MODE
              value: "standalone"
            - name: AI_PROVIDER
              value: "azure"
            - name: SHUTDOWN_TIMEOUT
              value: "60000"  # Longer shutdown for workers
          envFrom:
            - secretRef:
                name: api-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 4001
            initialDelaySeconds: 15
            periodSeconds: 60
