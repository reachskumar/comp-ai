apiVersion: v1
kind: ServiceAccount
metadata:
  name: compportiq-api
  namespace: compportiq
  annotations:
    # IRSA: link to AWS IAM role for Secrets Manager access
    eks.amazonaws.com/role-arn: PLACEHOLDER_IRSA_ROLE_ARN

