"use client";

import { GitCompare } from "lucide-react";
import { PlaceholderPage } from "@/components/placeholder-page";

export default function CalibrationPage() {
  return (
    <PlaceholderPage
      title="Calibration"
      description="Calibrate compensation across teams and departments."
      icon={GitCompare}
    />
  );
}

