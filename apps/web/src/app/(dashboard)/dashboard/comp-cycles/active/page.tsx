"use client";

import { ListChecks } from "lucide-react";
import { PlaceholderPage } from "@/components/placeholder-page";

export default function ActiveCyclesPage() {
  return (
    <PlaceholderPage
      title="Active Cycles"
      description="View and manage active compensation cycles."
      icon={ListChecks}
    />
  );
}

