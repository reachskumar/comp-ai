"use client";

import { FlaskConical } from "lucide-react";
import { PlaceholderPage } from "@/components/placeholder-page";

export default function SimulatorPage() {
  return (
    <PlaceholderPage
      title="Simulator"
      description="Run compensation rule simulations with different scenarios."
      icon={FlaskConical}
    />
  );
}

